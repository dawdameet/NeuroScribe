V.0.2.0
✅ Batch Normalization: Helps stabilize training and improve convergence.
✅ LeakyReLU: Prevents neurons from becoming inactive (dying ReLU problem).
✅ Dropout Increased: Helps prevent overfitting.
✅ Early Stopping: Stops training if validation loss stops improving.
✅ Model Saving: Saves the best model automatically.
✅ More Flexibility: Now supports different activation functions, optimizers, and dropout values in YAML.
This will train faster, generalize better, and reduce overfitting. 🚀🔥

V.0.3.0
✅ Xavier/He Weight Initialization for better training.
✅ ReduceLROnPlateau to dynamically adjust learning rate.
✅ Gradient Clipping to prevent exploding gradients.
✅ AdamW Optimizer with Weight Decay for better regularization.
✅ Dataset Switching (MNIST, CIFAR-10, etc.).
✅ Augmented Data for CIFAR-10 (rotation, flipping).
✅ Logging & Debugging Improvements.

This setup will train faster, generalize better, and reduce overfitting significantly! 🚀🔥 

V1.0.0
✅ Define any neural network model in YAML (without touching Python).
✅ Use the model in Python AI projects (e.g., training, inference, saving/loading).
✅ Modify models just by changing YAML (no need to rewrite Python code).


V1.0.1
✅ Categorical columns are encoded.
✅ Missing values are handled.
✅ Labels are reshaped to avoid PyTorch tensor shape issues.
✅ Validation split is correctly applied.

--------------- FOR selfsuff.yaml --------------------
✅ No Python changes required – model architecture, optimizer, loss function, and dataset completely controlled by YAML.
✅ Switching from a regression task (house prices) to a  classification task (MNIST digits) by just modifying YAML.
✅ Demonstrates self-sufficiency of your YAML-driven AI language.